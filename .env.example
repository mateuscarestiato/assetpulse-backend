# Configurações do Ambiente AssetPulse API
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000
HOST=0.0.0.0

# Caminho de conexão do banco de dados (padrão SQLite local)
# DATABASE_URL=sqlite:///database.db
